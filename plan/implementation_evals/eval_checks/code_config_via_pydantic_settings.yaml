schema_version: 1
id: code_config_via_pydantic_settings
type: harness_judge
description: All configuration enters through pydantic-settings; secrets are SecretStr/SecretBytes unwrapped only at point of use; direct env access only via a reasoned per-line exception.
tags: [code, requirements]
instructions: |
  You are judging whether this repository's IMPLEMENTATION honors a binding convention.
  Read plan/requirements/requirements_v3.md §Code — the binding source — then inspect the
  settings module(s) and grep the source for environment access under src/.

  Verify ALL of the following:
  1. Environment variables are read via pydantic-settings BaseSettings classes; the ruff
     TID251 ban on os.environ/os.getenv is configured in pyproject.toml. A direct env access
     is acceptable ONLY as the requirement's documented exception: a per-line ignore
     carrying a written reason — an unexplained or un-ignored direct access fails, as do
     evasions (importing environ under an alias, reading /proc/self/environ).
  2. Every secret-bearing setting (DB URLs with passwords, API keys, tokens) is typed
     SecretStr/SecretBytes, and .get_secret_value() appears only at the call sites that hand
     the value to a client/driver — never stored unwrapped on another object, never logged.
  3. Settings objects are constructed at composition boundaries (entry points/workers), not
     scattered ad hoc through domain logic.

  Source precedence: the design docs under plan/designs/ are the binding statement of the
  current system; the decision log is its indexed record — read cited decisions together
  with their refinement/withdrawal annotations. If sources appear to disagree, treat the
  design docs (including postgres_schema_design.md) as controlling, and note the
  discrepancy in your reason rather than failing an implementation that follows the
  current design.

  Score 1 only if EVERY condition demonstrably holds — cite file paths in your reason.
  Otherwise score 0.
