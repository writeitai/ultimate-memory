enabled: true
priority: 90
run_every: 1
must_follow: eval_reviewer
not_before_iteration: 1
emits_goal_check: true
description: |
  Run the session checks and publish the evidence-bound eval receipt,
  goal-check projection, and terminal control when all checks pass.
