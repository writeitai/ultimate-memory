enabled: true
priority: 10
run_every: 1
must_follow: planner
not_before_iteration: 1
description: |
  Create one child-session request for the selected work package or import
  terminal child-session evidence back into PM state.
